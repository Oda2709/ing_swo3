import 'package:flutter/material.dart';

class SensorP extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sensor Proximidad'),
      ),
      body: Center(
        child: Text('Sensores'),
      ),
    );
  }
}
